name: Keep Render Awake

on:
  schedule:
    - cron: "*/5 * * * *"   # runs every 10 minutes
  workflow_dispatch:         # lets you trigger manually

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: |
          curl -s https://lms-project-mern-49ws.onrender.com > /dev/null
